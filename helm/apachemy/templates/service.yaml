apiVersion: v1
kind: Service
metadata:
  name: {{ include "apachemy.fullname" . }}
  labels:
    {{- include "apachemy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apachemy.selectorLabels" . | nindent 4 }}
