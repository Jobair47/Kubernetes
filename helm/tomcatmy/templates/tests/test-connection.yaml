apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tomcatmy.fullname" . }}-test-connection"
  labels:
    {{- include "tomcatmy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tomcatmy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
